---
import { portfolio } from '../content/portfolio';
---

<section class="section" id="portfolio">
  <div class="max-w-6xl mx-auto">
    <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4 mb-8">
      <div>
        <p class="text-sm uppercase tracking-[0.2em] text-slate-400">Samples</p>
        <h2 class="text-3xl md:text-4xl font-semibold text-white">Proof that travels well.</h2>
      </div>
      <p class="max-w-xl text-slate-200">
        Representative work across niches we know: tech, compliance, veteran, and career storytelling. Full samples available on request.
      </p>
    </div>
    <div class="grid md:grid-cols-3 gap-6">
      {portfolio.map((item) => (
        <article class="glass rounded-2xl p-6 border border-white/10 shadow-card space-y-3" key={item.title}>
          <p class="text-xs uppercase tracking-[0.2em] text-slate-400">{item.niche}</p>
          <h3 class="text-xl font-semibold text-white">{item.title}</h3>
          <p class="text-sm text-slate-200">{item.format}</p>
          <p class="text-sm text-slate-300">{item.outcome}</p>
          <p class="text-xs text-slate-400">Tone: {item.tone}</p>
          <a href="#contact" class="inline-flex items-center gap-2 text-signal-400 font-semibold hover:text-signal-300 transition-colors">
            Request full sample
            <span aria-hidden="true">→</span>
          </a>
        </article>
      ))}
    </div>
  </div>
</section>
